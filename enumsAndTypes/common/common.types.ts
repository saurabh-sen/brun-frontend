// export a enum for button type 'submit' and 'button'
enum ButtonType {
    SUBMIT = "submit",
    BUTTON = "button"
}

enum RoleEnum {
    ADMIN = "admin",
    USER = "user"
}

export { ButtonType, RoleEnum }